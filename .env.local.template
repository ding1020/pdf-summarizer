# 30分钟配置指南

## 第一步：配置 Clerk 认证 (~10分钟)

### 1.1 注册/登录 Clerk
访问: https://dashboard.clerk.com

### 1.2 创建应用
1. 点击 "Add Application"
2. 选择登录方式（建议勾选 Email, Google, GitHub）
3. 应用名称随意，如 "PDF Summarizer"
4. 点击 "Create Application"

### 1.3 获取 API Keys
1. 进入应用后，点击左侧 "API Keys"
2. 复制以下值：
   - **NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY** → 如 `pk_test_xxxxxx`
   - **CLERK_SECRET_KEY** → 如 `sk_test_xxxxxx`

### 1.4 配置 URLs
1. 点击左侧 "URNs" (或 Settings > URLs)
2. 设置：
   - **Allowed Origins**: `http://localhost:3000` (开发), `https://你的域名.com` (生产)
   - **Sign-in URL**: `/sign-in`
   - **Sign-up URL**: `/sign-up`

---

## 第二步：配置 Groq AI (~5分钟)

### 2.1 注册 Groq
访问: https://console.groq.com

### 2.2 创建 API Key
1. 使用 Google/GitHub 登录
2. 点击左侧 "API Keys"
3. 点击 "Create API Key"
4. 名称随意，如 "pdf-summarizer"
5. 复制 Key → `GROQ_API_KEY` 如 `gsk_xxxxxx`

**注意**: Groq 免费额度为 30请求/分钟，足够个人/小团队使用

---

## 第三步：配置 Supabase 数据库 (~10分钟)

### 3.1 注册 Supabase
访问: https://supabase.com

### 3.2 创建项目
1. 点击 "New Project"
2. 填写：
   - **Name**: pdf-summarizer
   - **Database Password**: 生成一个强密码（保存好）
   - **Region**: 选择离你最近的
3. 点击 "Create new project"
4. **等待2-3分钟** 直到状态变为 "Up"

### 3.3 获取连接字符串
1. 进入项目 → Settings → Database
2. 滚动到 "Connection string" 部分
3. 点击 "URI" tab
4. 复制完整连接字符串
5. 格式如: `postgresql://postgres.[REF]:[PASSWORD]@db.[REF].supabase.co:5432/postgres`

---

## 第四步：配置 Paddle 支付 (~20分钟)

### 4.1 注册 Paddle Vendor
访问: https://paddle.com
- 点击 "Start selling"
- 填写企业信息（个人可用个人账户）

### 4.2 获取 Vendor ID 和 API Key
1. 进入 Dashboard → Developer Tools → Authentication
2. 复制：
   - **Vendor ID** → 如 `12345`
   - **API Key** → 如 `paddle_live_xxxxxx`
   - **Webhook Key** → 如 `paddle_live_xxxxxx`

### 4.3 创建产品
1. 进入 Dashboard → Catalog → Products
2. 点击 "New Product"
3. 填写：
   - **Name**: Pro Monthly
   - **Type**: Recurring
   - **Billing Cycle**: Monthly
   - **Unit Amount**: 9 (USD)
4. 点击 "Create Product"
5. 点击 "Create Price" → 复制 **Price ID** → `pri_xxxxxx`

### 4.4 创建年付产品
重复上面步骤，创建：
- Name: Pro Yearly
- Billing Cycle: Annual
- Unit Amount: 79
- Price ID: `pri_xxxxxx`

### 4.5 配置 Webhook
1. 进入 Dashboard → Developer Tools → Notifications
2. 点击 "Add Webhook"
3. URL: `https://你的域名.com/api/webhooks/paddle`
4. 选择 events:
   - `subscription.created`
   - `subscription.updated`
   - `subscription.canceled`
   - `transaction.completed`
5. 点击 "Enable Webhook"

---

## 第五步：配置 .env.local
<minimax:tool_call>
<invoke name="write_to_file">
<parameter name="content"># ===========================================
# PDF Summarizer - 你的配置文件
# ===========================================
# 按照 QUICK_SETUP.md 的步骤配置后，把值填在这里
# ===========================================

# ============ CLERK (认证) ============
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_在这里填入你的Clerk公钥
CLERK_SECRET_KEY=sk_test_在这里填入你的Clerk密钥
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# ============ DATABASE (Supabase PostgreSQL) ============
DATABASE_URL=postgresql://在这里填入你的Supabase连接字符串

# ============ AI PROVIDER (Groq) ============
GROQ_API_KEY=gsk_在这里填入你的Groq密钥

# ============ PADDLE (支付) ============
# 开发环境 (测试模式)
PADDLE_ENV=sandbox
PADDLE_VENDOR_ID=在这里填入你的Vendor ID
PADDLE_SECRET_KEY=paddle_test_在这里填入你的API密钥
PADDLE_WEBHOOK_SECRET=paddle_test_在这里填入你的Webhook密钥

# Paddle Price IDs
PADDLE_PRICE_ID=pri_在这里填入你的月付Price ID
PADDLE_PRICE_ID_YEARLY=pri_在这里填入你的年付Price ID

# ============ 应用配置 ============
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPPORT_EMAIL=support@你的域名.com

# ============ 限流配置 (默认即可) ============
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_FREE=20
RATE_LIMIT_MAX_PRO=60

# ============ 日志 Key (随机生成) ============
# 运行: openssl rand -hex 32
LOG_API_KEY=在这里运行openssl生成一个随机密钥填入

# ===========================================
# 生产环境变量 (稍后配置)
# ===========================================
# 当你准备好上线时，复制这个文件为 .env.production
# 然后将 sandbox 改为 production，将 test 改为 live
